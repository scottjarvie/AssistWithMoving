const scenarios = [
  "PCS mixed move",
  "normal household move",
  "storage manifest",
  "donation pickup",
  "insurance claim packet",
];

console.log("MovingManifest seed skeleton");
console.log("No data was written. Convex seed writes will be added after schema exists.");
console.log("Planned scenarios:");

for (const scenario of scenarios) {
  console.log(`- ${scenario}`);
}
