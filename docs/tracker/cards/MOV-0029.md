---
id: MOV-0029
title: Prove and keep production MCP acceptance repeatable
status: doing
type: operations
area: ai-access
priority: P1
work-orders: MOV-WO-006
created: 2026-08-12
updated: 2026-08-12
updated-by: Codex
---

## Why this exists

Moving's chosen-AI promise needs one repeatable live proof that crosses real
Clerk consent, the released remote MCP, durable move work, normal UI reflection,
and complete cleanup without using a real household.

## Current truth

The approved non-privileged **Moving MCP Test** identity now exists only in the
MovingManifest production Clerk instance. Its bootstrap password was randomized
and discarded. A first official-SDK attempt reached real consent and token
exchange, then the released `/mcp` rejected Clerk's valid access token because
production DCR tokens omit `aud`. No MCP data write occurred, and the refresh
token was revoked. A focused repair accepts Clerk's observed no-audience token
shape while retaining exact issuer, signature, expiry, `at+jwt`, subject, and
client-id verification and rejecting unrelated supplied audiences.

**Priority:** Now. **Change class:** authentication software, live synthetic
acceptance, provider cleanup, and release truth.

## Next safe action

Release the verifier repair through a protected PR, rerun the same marked
official-SDK workflow, confirm the source-backed result in the Move overview,
then purge the move and revoke/delete every temporary token, client, session,
and grant. Retain only the test identity and documented one-time sign-in recipe.

## Constraints

- Use only `moving-mcp-e2e+production@example.com`; never use Scott's or another
  person's account.
- Every move and OAuth client must begin with the documented `E2E PROD` marker.
- Do not inspect or mutate another household, real move, or real Queue record.
- Queue transitions remain outside canonical OAuth acceptance.
- Keep local, PR/CI, deployment, authenticated MCP, UI, revocation, and purge
  receipts separate.

## Completion evidence

- [x] Moving-only production test identity created without elevated roles.
- [x] Real production consent and token exchange reproduced the missing-`aud`
  incompatibility; no MCP write occurred and the refresh token was revoked.
- [x] Focused verifier regression tests pass for absent, resource, client-id,
  wrong, and expired audience shapes.
- [ ] Protected repair is merged and deployed to production.
- [ ] Official SDK completes brief → source-backed result → replay → search →
  granular correction → hydration → updated brief.
- [ ] Normal Move overview shows the saved result and MCP provenance.
- [ ] Temporary OAuth/session/grant state and every marked Move/MCP/Queue record
  are proven removed while the test identity remains.

## History

- 2026-08-12 · Scott via coordinator task — explicitly authorized one retained
  non-privileged Moving production test identity and removable live MCP/UI proof.
- 2026-08-12 · Codex — created the isolated identity and move, reproduced the
  production OAuth incompatibility before any MCP data write, revoked the
  refresh token, and began the bounded verifier repair.
